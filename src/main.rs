fn main() {
    // major version number
    let vmajor = 0;
    // minor version number
    let vminor = 1;
    // bugfix version number
    let vfix = 0;
    println!("ECTerm v{0}.{1}.{2}", vmajor, vminor, vfix);
}